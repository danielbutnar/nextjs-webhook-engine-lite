{
  "name": "nextjs-webhook-engine-lite",
  "version": "1.0.0",
  "description": "Production reference implementation for secure, idempotent serverless webhook ingestion with Next.js 15 and Redis REST.",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test:attack": "tsx --env-file=.env.local scripts/simulate-replay.ts || tsx scripts/simulate-replay.ts"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}